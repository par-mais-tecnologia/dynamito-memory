/* eslint max-nested-callbacks:0 */
import DynamitoMemory from '../';

describe('Dynamito Memory: ', () => {
  it('should module to be loaded', () => {
    expect(DynamitoMemory).not.to.be.equal(undefined);
  });
});
